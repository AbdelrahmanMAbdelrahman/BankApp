import { Component } from '@angular/core';
import { RouterLink, RouterLinkActive } from "@angular/router";

@Component({
  selector: 'app-contract-side-bar',
  imports: [RouterLink],
  templateUrl: './contract-side-bar.html',
  styleUrl: './contract-side-bar.css',
})
export class ContractSideBar {}
